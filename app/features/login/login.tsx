import { Button } from '@components/ui/button'

export function Login() {
  return (
    <main>
      <div>
        <div>
          <nav>
            <Button variant={'default'} size={'default'}>
              Login
            </Button>
          </nav>
        </div>
      </div>
    </main>
  )
}
